fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic[email]
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
python-dotenv==1.0.1
pyjwt==2.9.0
# We hash passwords directly with the `bcrypt` package's own API — see the
# module docstring in app/utils/security.py for why passlib is deliberately
# NOT used here (it has a known, unfixable-by-pinning incompatibility with
# modern bcrypt releases that raises "password cannot be longer than 72
# bytes" even for ordinary short passwords, because passlib is unmaintained
# and its bcrypt-backend self-test predates bcrypt's current API).
bcrypt==4.2.0
python-multipart==0.0.9

numpy==2.1.1
pandas==2.2.3
scipy==1.14.1
scikit-learn==1.5.2
xgboost==2.1.1

pytest==8.3.3
httpx==0.27.2

# Optional, only needed if AI_PROVIDER=anthropic
anthropic==0.34.2
